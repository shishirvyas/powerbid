# ---- Local development ----
NODE_ENV=development

# Worker / API (apps/api/.dev.vars)
JWT_SECRET=change-me-in-prod
JWT_ISSUER=bid
APP_URL=http://localhost:5173
MAIL_FROM=no-reply@lanengineering.in
MAIL_FROM_NAME=BID

# Batch 4 dispatch (used by app-next)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@lanengineering.in

WHATSAPP_PROVIDER=wa_me
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
DISPATCH_WEBHOOK_SECRET=change-me

# Frontend (apps/web/.env)
VITE_API_BASE_URL=http://localhost:8787
